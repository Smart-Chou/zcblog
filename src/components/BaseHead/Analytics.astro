---
import { config, umami, site } from "~/config";

const gaId = config.ga ?? null;

const gtagScript = gaId
    ? `window.dataLayer = window.dataLayer || [];
function gtag() {
    dataLayer.push(arguments);
}
gtag("js", new Date());
gtag("config", "${gaId}");`
    : null;
---

<!-- Analytics --><!-- Umami -->{
    umami.enable && (
        <>
            <script
                is:inline
                type="text/partytown"
                src={`${umami.umamiBaseUrl}/script.js`}
                defer
                data-website-id={String(umami.umamiId)}
                data-domains={site.domains.join(",")}
            />
        </>
    )
}

<!-- Google Analytics -->
{
    gaId && (
        <>
            <script
                is:inline
                type="text/partytown"
                src={`https://www.googletagmanager.com/gtag/js?id=${gaId}`}
            />
            <script is:inline type="text/partytown" set:html={gtagScript}></script>
        </>
    )
}
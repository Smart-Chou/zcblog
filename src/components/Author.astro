---
import { Icon } from "astro-icon/components";
import { author, categories, socialLinks } from "~/self.config";
---

<div class="author">
    <img
        src={`/${author.avatar}`}
        alt="Author"
        class="author__image"
        width="180"
        loading="eager"
        decoding="async"
    />
    <h1 class="author__site-title">{author.type}</h1>
    <p class="author__intro">{author.motto}</p>
    <p class="author__links">
        {
            categories.map((categorie) => (
                <a
                    href={categorie.url}
                    title={categorie.title}
                    rel="noopener noreferrer"
                    class="has-tooltip"
                    style={categorie.style}
                    aria-label={categorie.title}
                >
                    <Icon name={categorie.svg} />
                </a>
            ))
        }
        <span>/</span>
        {
            socialLinks.map((social) => (
                <a
                    data-key={`social-${social.title}`}
                    href={social.url}
                    target="_blank"
                    title={social.title}
                    rel="noopener noreferrer"
                    class="has-tooltip"
                    style={social.style}
                    aria-label={social.title}
                >
                    <Icon name={social.svg} />
                </a>
            ))
        }
    </p>
</div>

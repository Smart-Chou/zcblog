---
import { getLocale } from "~/utils/i18n";
import ArchivesContent from "~/components/Pages/ArchivesContent.astro";
const locale = getLocale(Astro.currentLocale || "zh");
---

<ArchivesContent locale={locale} />

---
import BaseLayout from "~/layouts/BaseLayout.astro";
import notFound from "~/assets/404.svg";
import { site } from "~/config";
import { t, getLocale, type Locale } from "~/utils/i18n";

const pathname = Astro.url?.pathname || "";
const loc: Locale = getLocale(pathname.startsWith("/en/") ? "en" : "zh");
---

<BaseLayout
    title={t(loc, "common.404.title")}
    description={`${site.title} - ${t(loc, "common.404.message")}`}
    image={Astro.site + site.coverImage}
    imageAlt="404"
    keywords="404"
    publishedTime={new Date()}
    modifiedTime={new Date()}
>
    <div>
        <h1 class="not-found-title text-center space-bottom">404</h1>
        <div class="not-found-container">
            <p class="description">
                🍌 Oh, no, not here.
                <a href="/" class="active">Take me back home. ↩</a>
            </p>
            <img
                src={notFound.src}
                alt="404 Not Found"
                class="not-found-bg--loaded"
                width="600"
                loading="lazy"
            />
        </div>
    </div>
</BaseLayout>

<style>
    .not-found-container {
        text-align: center;
        max-width: var(--content-width);
        margin: 0 auto;
    }

    .not-found-container .description {
        opacity: 0.8;
    }
</style>

---
import { getLocale } from "~/utils/i18n";
import DonateContent from "~/components/Pages/DonateContent.astro";
const locale = getLocale(Astro.currentLocale || "zh");
---

<DonateContent locale={locale} />

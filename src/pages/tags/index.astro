---
import { getLocale } from "~/utils/i18n";
import TagsIndexContent from "~/components/Pages/TagsIndexContent.astro";
const locale = getLocale(Astro.currentLocale || "zh");
---

<TagsIndexContent locale={locale} />
